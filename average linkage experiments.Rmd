---
title: "Average Linkage Experiments"
author: "Justin Lin for ISCC"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(igraph)

source("Algorithms/DR assessment tool algs.R")
source("Algorithms/single-linkage graph")
```

```{r}
X = matrix(c(0,2,0,0,1,0,6,0,6,6), ncol=2, byrow=TRUE)
plot(X)
```

```{r}
h = get_mst(X)
g = get_avg_linkage_graph(X)
```

```{r}
plot(h, layout=layout_with_kk)
plot(g, layout=layout_with_kk)
```

